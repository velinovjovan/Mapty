# Mapty 🗺️

An interactive workout tracking app built with **vanilla JavaScript** and the **Leaflet library**. Mapty allows users to log running and cycling workouts directly on an interactive map, showcasing object-oriented programming and geolocation features.

---

## 🚴 Features

- **Geolocation API** to get the user’s current position
- **Interactive map** using Leaflet.js
- Add workouts by clicking on the map
- Log **running** or **cycling** workouts with details like distance, duration, cadence, and elevation gain
- Display all workouts in a sidebar list
- Automatically move the map to the workout location when clicked
- **Local storage** support for saving and loading workouts

---

## 🧠 Key Concepts Practiced

- **Object-Oriented Programming (OOP)** in JavaScript
- Using **classes** and **inheritance** for workout types
- Handling **geolocation** and map rendering
- Integrating **Leaflet.js** for mapping functionality
- Managing **data persistence** with `localStorage`
- Efficient **event handling** and DOM manipulation

---

## 🚀 How to Run

1. Clone this repository:

   ```bash
   git clone https://github.com/velinovjovan/Mapty.git
   ```

2. Open the project folder and launch the `index.html` file in your browser.

Make sure you have an active internet connection for the **Leaflet** map to load.
